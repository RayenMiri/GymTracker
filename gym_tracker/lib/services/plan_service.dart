import 'package:flutter/material.dart';

class PlanService extends StatelessWidget {
  const PlanService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('plan_service'),
    );
  }
}
